import React from "react"
import ReactDOM from "react-dom"
import App from "./App"

window.addEventListener("DOMContentLoaded", function (e) {
  const root = ReactDOM.createRoot(document.getElementById("pv_sync"))
  root.render(<App />)
})
